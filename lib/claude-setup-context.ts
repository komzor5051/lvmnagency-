/**
 * Влад's actual Claude Code setup — skills, MCP servers, workflows he
 * really uses day to day. Used by the writer to ground "Claude Code для
 * разработчиков" pillar articles in real, verifiable specifics instead of
 * generic advice pulled from web research.
 *
 * Keep this file GENERIC and CLIENT-FREE: only tools/skills/patterns that
 * are safe to describe publicly. Do not add client names, client-specific
 * MCP servers (e.g. an account tied to one client), internal business
 * tooling, or revenue/strategy details. Update as the real setup evolves.
 */

export const CLAUDE_SETUP_CONTEXT = `
## Реальный Claude Code setup Влада (используй как материал для статей про Claude Code)

Это не абстрактные советы "из интернета" — это то, чем Влад реально пользуется каждый день.
Бери конкретику отсюда, когда пишешь статью из пилла "Claude Code для разработчиков":
называй вещи их настоящими именами (skills, MCP, subagents, hooks), но не выдумывай
деталей сверх того, что здесь описано.

### Skills (переиспользуемые процессы, вызываются по названию)
- Superpowers — набор process-скиллов: brainstorming (проработка идеи вопросами перед
  реализацией), systematic-debugging (метод для багов), writing-plans (пошаговый план
  реализации из спеки), subagent-driven-development (исполнение плана волной субагентов
  с ревью после каждой задачи), test-driven-development, using-git-worktrees, code-review,
  finishing-a-development-branch.
- Кастомные скиллы для конкретных задач: например, дизайн-ревью, разбор конкурентов,
  ресерч (deep-research), генерация визуальных объяснений (visual-explainer).

### Отдельные прикладные скиллы (стоят у Влада, факты — из их реальных SKILL.md)
- video-use — монтаж видео разговором: транскрибирует материал (word-level ASR через
  ElevenLabs Scribe), предлагает стратегию монтажа простым языком, после подтверждения
  режет по границам слов, клеит сегменты без перекодирования, накладывает цветокор,
  анимации (ffmpeg + PIL, HyperFrames, Remotion, Manim) и субтитры. Жёсткие правила
  производства: субтитры накладываются последними, 30-мс аудио-фейды на каждом стыке,
  никогда не резать внутри слова, паддинг 30-200 мс на краях склейки. Решения о монтаже
  хранит в edl.json, память проекта — в project.md. Сам проверяет свой результат
  (кадры на стыках, длительность) до показа человеку.
- seo — комплексный SEO-анализ: оркестратор из 24 суб-скиллов и 18 субагентов.
  Команды вида /seo audit <url> (полный аудит с параллельными субагентами: техничка,
  контент/E-E-A-T, schema, sitemap, Core Web Vitals, GEO для AI Overviews/ChatGPT/
  Perplexity), /seo page, /seo schema, /seo geo, /seo cluster. Сам определяет тип
  бизнеса (SaaS, e-commerce, локальный, издатель) и подключает нужные проверки.
  Итог — SEO Health Score 0-100 (веса: контент 23%, техничка 22%, on-page 20%) и
  план действий по приоритетам Critical/High/Medium/Low. Знает актуальные правила:
  INP вместо FID, FAQ-разметка больше не даёт rich results в Google.
- instagram-marketing — контент-операции для Instagram: 9 узких суб-скиллов —
  капшены с хуком в первых 125 символах (дальше «ещё» скрывает текст), пошаговое
  планирование каруселей (2-10 слайдов через API), реверс-инжиниринг хуков из
  вирусных Reels, хэштеги по схеме 3-5 штук (ниша/середина/широкие) вместо 30,
  контент-план на неделю, аудит профиля. Паттерн: черновик → аппрув человека →
  публикация с медиа (Instagram не принимает посты без картинки/видео) через
  Publora API. Голосовые правила: без длинных тире, конкретные числа вместо
  прилагательных, одна мысль на пост.
- humanizer — вычищает следы ИИ-текста по гайду Википедии «Signs of AI writing»:
  раздутая значимость («знаковый момент», «подчёркивает важность»), рекламный тон,
  фальшивая глубина через деепричастные хвосты, «правило трёх», AI-словарь (leverage,
  delve, unlock), злоупотребление тире, рубленые фрагменты по 2-3 слова. Вторая
  половина работы — добавить голос: мнения, ритм, первое лицо, признание сложности.
  Финальный проход — «что здесь всё ещё выдаёт ИИ?» и правка по ответу.
- Скиллы триггерятся по описанию задачи, а не только по явной команде — Claude Code сам
  решает, когда какой скилл применить.

### MCP-серверы (расширяют Claude Code внешними инструментами)
- Supabase MCP — прямой доступ к базе: миграции, SQL-запросы, чтение схемы, advisors.
- Playwright MCP — управление браузером (навигация, клики, скриншоты, чтение DOM) для
  QA живых страниц.
- Context7 MCP — актуальная документация библиотек/фреймворков вместо устаревших знаний
  из обучающих данных.
- n8n MCP — поиск нод, генерация workflow-кода, валидация workflow напрямую из чата.
- Figma MCP — чтение/запись дизайна: получение контекста компонентов, генерация UI из
  макета и обратно.
- Linear MCP — работа с issues/проектами трекера прямо из разработки.
- shadcn MCP — поиск и установка UI-компонентов по описанию задачи.

### Субагенты и оркестрация
- Agent-инструмент — можно делегировать задачу отдельному субагенту с чистым контекстом
  (например: "исследуй кодовую базу", "проведи ревью кода") — результат возвращается,
  контекст основной сессии не засоряется.
- Workflow-инструмент — детерминированные многоагентные сценарии в коде (не прозе):
  pipeline() гонит задачи через стадии без барьера между ними, parallel() ждёт все ветки
  разом. Используется для масштабного ресерча, ревью по нескольким независимым линзам,
  массовых рефакторингов.
- Git worktrees — изоляция параллельной работы агентов в отдельных копиях репозитория,
  чтобы одновременные правки не конфликтовали.

### Память и контекст
- Персистентная файловая память между сессиями (не только контекст одного чата) — типы:
  факты о пользователе, feedback (как правильно/неправильно работать), project (контекст
  текущих задач), reference (где искать внешнюю информацию).
- CLAUDE.md — файлы с инструкциями по проекту, которые Claude Code читает автоматически
  на разных уровнях (глобально, по проекту, по под-папке).
- Hooks — скрипты, запускающиеся на события (например, на старте сессии подтягивают
  контекст из внешних источников).

### Как реально устроена работа
- План согласуется до кода: сначала бриф/спека, потом пошаговый план, потом исполнение —
  не пишем код с первого сообщения на сложных задачах.
- Ревью — отдельный шаг, не часть реализации: свежий агент без истории проверяет диф на
  соответствие требованиям и качество кода, отдельно от того, кто писал код.
- Задачи трекаются явно (списком todo), а не держатся в голове — по мере выполнения
  статусы обновляются, это видно и человеку, и следующему агенту.
`;
